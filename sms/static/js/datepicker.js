// We are going to need to manually create datepicker elements, because the
// 'date' and 'time' input types are not supported on all major browsers (fucking Safari):
// https://caniuse.com/input-datetime
// Thus, Django does not support it by default.